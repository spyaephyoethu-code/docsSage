# KB validators: FastAPI dependencies for request-level checks that go beyond
# Pydantic field constraints (e.g. file size, content-type, multipart guards).
# Quota enforcement lives in KBService, not here.
